<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="view_crm_lead_form_inherit_contacts" model="ir.ui.view">
        <field name="name">crm.lead.form.inherit.contacts</field>
        <field name="model">crm.lead</field>
        <field name="inherit_id" ref="crm.crm_lead_view_form"/>
        <field name="arch" type="xml">

            <xpath expr="//notebook" position="inside">

                <page string="Contacts">

                    <field name="contact_ids"
                           mode="kanban"
                           context="{
                               'default_parent_id': partner_id,
                               'default_company_type': 'person',
                               'default_is_company': False,
                               'default_type': 'contact'
                           }">

                        <kanban>

                            <field name="name"/>
                            <field name="function"/>
                            <field name="phone"/>
                            <field name="mobile"/>
                            <field name="email"/>
                            <field name="image_128"/>

                            <templates>
                                <t t-name="kanban-box">
                                    <div class="oe_kanban_global_click o_kanban_record">

                                        <div class="o_kanban_image">
                                            <img t-att-src="kanban_image('res.partner','image_128',record.id.raw_value)"
                                                 alt="Contact"/>
                                        </div>

                                        <div class="o_kanban_details">
                                            <strong>
                                                <field name="name"/>
                                            </strong>

                                            <div t-if="record.function.raw_value">
                                                <field name="function"/>
                                            </div>

                                            <div t-if="record.email.raw_value">
                                                <field name="email" widget="email"/>
                                            </div>

                                            <div t-if="record.phone.raw_value">
                                                Phone: <t t-esc="record.phone.value"/>
                                            </div>

                                            <div t-if="record.mobile.raw_value">
                                                Mobile: <t t-esc="record.mobile.value"/>
                                            </div>

                                        </div>

                                    </div>
                                </t>
                            </templates>

                        </kanban>

                    </field>

                </page>

            </xpath>

        </field>
    </record>

</odoo>
